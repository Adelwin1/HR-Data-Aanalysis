# HR-Data-Aanalysis
Python project for cleaning and analyzing messy HR data. Uses pandas to handle missing values, convert data types, and strip whitespace. Includes the raw and cleaned CSV files, with analysis.py demonstrating practical data preprocessing techniques.
